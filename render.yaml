services:
  - type: web
    name: farsitranscriber
    env: node
    branch: server
    buildCommand: cd server && npm install
    startCommand: cd server && node server.js
    envVars:
      - key: OPENAI_API_KEY
        sync: false
